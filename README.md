# 🎮 База данных для игры "Ведьмак 3"

Простая база данных для отслеживания игры.

![alt text](<1234.png>)

## 📋 Таблицы

### 1. Игроки
```sql
CREATE TABLE игроки (
    id INT PRIMARY KEY,
    имя VARCHAR(50),
    почта VARCHAR(100)
);
```

### 2. Персонажи
```sql
CREATE TABLE персонажи (
    id INT PRIMARY KEY,
    id_игрока INT,
    имя VARCHAR(50),
    уровень INT
);
```

### 3. Квесты
```sql
CREATE TABLE квесты (
    id INT PRIMARY KEY,
    название VARCHAR(100),
    тип VARCHAR(50)
);
```

### 4. Предметы
```sql
CREATE TABLE предметы (
    id INT PRIMARY KEY,
    название VARCHAR(100),
    тип VARCHAR(50)
);
```

## 🔗 Связи между таблицами

1. **Игрок → Персонажи** (1 ко многим)
   - Один игрок может создать много персонажей

2. **Персонаж → Инвентарь** (1 ко многим)
   - У каждого персонажа свой инвентарь

3. **Персонаж → Квесты** (многие ко многим)
   - Персонаж может выполнять много квестов
   - Квест могут выполнять много персонажей

## 📊 Примеры данных

### Игроки:
```sql
INSERT INTO игроки VALUES 
(1, 'Алексей', 'alex@mail.ru'),
(2, 'Мария', 'maria@yandex.ru');
```

### Персонажи:
```sql
INSERT INTO персонажи VALUES
(1, 1, 'Геральт', 35),
(2, 2, 'Цири', 30);
```

### Квесты:
```sql
INSERT INTO квесты VALUES
(1, 'Кровавый барон', 'Основной'),
(2, 'Охота на грифона', 'Контракт');
```

### Предметы:
```sql
INSERT INTO предметы VALUES
(1, 'Серебряный меч', 'Оружие'),
(2, 'Броня', 'Защита');
```

## 🔍 Простые запросы

### 1. Все персонажи игрока
```sql
SELECT * FROM персонажи 
WHERE id_игрока = 1;
```

### 2. Найти предметы
```sql
SELECT * FROM предметы 
WHERE тип = 'Оружие';
```

### 3. Квесты по типу
```sql
SELECT * FROM квесты 
WHERE тип = 'Основной';
```

## 🎯 Как использовать

1. **Создать базу:**
```sql
CREATE DATABASE игра;
USE игра;
```

2. **Создать таблицы** (по порядку из раздела выше)

3. **Добавить данные** (как в примерах)

4. **Выполнять запросы** для получения информации

---

## 💡 Пример работы

**Задача:** Алексей создал персонажа Геральта и хочет начать квест "Кровавый барон"

**Что нужно сделать:**
1. Добавить запись в таблицу персонажей
2. Добавить запись о начале квеста
3. Выдать начальные предметы
